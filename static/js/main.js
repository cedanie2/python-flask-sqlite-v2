/*
    Simple JavaScript function to display a confirmation message
    when the user attempts to reset the data in the table of the 
    database.db SQLite database
*/
function confirm_submit(){
   alert("Your database will now be reset.");
}